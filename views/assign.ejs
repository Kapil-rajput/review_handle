<div class="nav">
  <div>
    <h3>Hello, <%= user.name%> !</h3>
  </div>
  <div class="menu-nav">
    <% if(user.isAdmin) {%>
    <a href="dashboard">Dashboard</a>
    <a href="addemployee">Add Employee</a>
    <a href="reviews">Reviews</a>
    <%}%> <% if(!user.isAdmin) {%>
    <a href="addreview">Give Review</a>
    <a href="feedback">Feedback</a>
    <%}%>
    <a href="/logout">Logout</a>
  </div>
</div>

<div class="addEmployee-Container">
  <div>
    <h3>Select Employee to Assign Feedback</h3>
  </div>
  <div>
    <form class="form-add-edit-delete" action="/assign" method="POST">
      <div>
        <select name="assignTo" id="name" required>
            <option value="">Assign To</option>
            <% Admins.forEach(function (admin){%>
                <% if(!admin.isAdmin) {%>
                 <option value="<%=admin.name%>"><%=admin.name%></option>
                    <%}%>
                 <%})%>
        </select>
    </div>
              <div>
        <select name="assignFor" id="name" required>
            <option value="">Assign For</option>
            <% Admins.forEach(function (admin){%>
                <% if(!admin.isAdmin) {%>
                 <option value="<%=admin.name%>"><%=admin.name%></option>
                    <%}%>
                 <%})%>
        </select>
    </div>
    <div>
        <button type="submit" class="btn btn-dark">Assign</button>
      </div>
    </form>
  </div>
  </div>
</div>
